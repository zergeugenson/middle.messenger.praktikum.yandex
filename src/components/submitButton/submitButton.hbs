<button
	{{#if datapage }}
	data-page="datapage"
	{{/if}}
	{{#if id }}
	id="{{ id }}"
	{{/if}}
	class="{{class}}{{# if disabled }} hidden{{/ if }}"
	type="{{#if type}}{{type}}{{else}}submit{{/if}}"
	{{#if disabled}} disabled{{/if}}
>
	{{text}}
</button>
